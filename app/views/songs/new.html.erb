
<div class='container jumbotron'>
  <%=binding.pry %>
  <h1><%= @song.id ? ('Edit Song') : ('Create Song') %></h1>
  <%= form_for @song do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :year %>
    <%= f.text_field :year %>
    <%= f.submit %>
  <% end %>
</div>
