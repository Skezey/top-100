<h1><%= @sub.id ? 'Edit Sub' : 'Create Song' %></h1>

<%= form_for @song do |f| %>
  <%= f.label :name %>
  <%= f.label :title %>
  <%= f.label :year %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>